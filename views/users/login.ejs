<% layout("layouts/boilerplate") %>

<div class="d-flex justify-content-center align-items-center vh-100 bg-light">
  <div class="card shadow-lg border-0 p-5 rounded-4" style="max-width: 420px; width: 100%;">
    
    <!-- 🏷 Header -->
    <div class="text-center mb-4">
      <h2 class="fw-bold text-dark">Welcome Back 👋</h2>
      <p class="text-muted mb-1">Log in to continue your journey with</p>
      <h4 class="fw-bold text-danger">WonderLust</h4>
    </div>

    <!-- 🧾 Login Form -->
    <form action="/login" method="POST" class="needs-validation" novalidate>

      <!-- 👤 Username -->
      <div class="mb-3">
        <label for="username" class="form-label fw-semibold">Username</label>
        <input 
          type="text" 
          id="username" 
          name="username" 
          class="form-control form-control-lg" 
          placeholder="Enter your username" 
          required>
        <div class="invalid-feedback">Please enter your username.</div>
      </div>

      <!-- 🔒 Password -->
      <div class="mb-4">
        <label for="password" class="form-label fw-semibold">Password</label>
        <input 
          type="password" 
          id="password" 
          name="password" 
          class="form-control form-control-lg" 
          placeholder="Enter your password" 
          required>
        <div class="invalid-feedback">Password is required.</div>
      </div>

      <!-- 🔘 Submit -->
      <button type="submit" class="btn btn-danger w-100 py-2 fw-semibold">
        Log In
      </button>

      <!-- Divider -->
      <div class="text-center text-muted small my-3">or</div>

      <!-- 🌐 Mock Social Buttons -->
      <div class="d-grid gap-2 mb-3">
        <button type="button" class="btn btn-outline-secondary">
          <i class="bi bi-google me-2"></i> Continue with Google
        </button>
        <button type="button" class="btn btn-outline-secondary">
          <i class="bi bi-facebook me-2"></i> Continue with Facebook
        </button>
      </div>

      <!-- 🔗 Redirect to Signup -->
      <p class="text-center mt-3 mb-0">
        Don’t have an account? 
        <a href="/signup" class="text-decoration-none text-danger fw-semibold">Sign up here</a>
      </p>

    </form>
  </div>
</div>
